{
  "name": "vscode-lsp-mcp-bridge",
  "displayName": "VS Code LSP MCP Bridge",
  "description": "Expose VS Code language intelligence as a local MCP server for AI coding tools.",
  "version": "0.2.8",
  "type": "module",
  "publisher": "georgiana-alba",
  "license": "MIT",
  "icon": "resources/icon.png",
  "preview": false,
  "pricing": "Free",
  "galleryBanner": {
    "color": "#101820",
    "theme": "dark"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/AlwaysHC/vscode-lsp-mcp-bridge.git"
  },
  "homepage": "https://github.com/AlwaysHC/vscode-lsp-mcp-bridge#readme",
  "bugs": {
    "url": "https://github.com/AlwaysHC/vscode-lsp-mcp-bridge/issues"
  },
  "categories": [
    "Programming Languages",
    "Other"
  ],
  "keywords": [
    "mcp",
    "lsp",
    "roslyn",
    "codex",
    "copilot",
    "claude",
    "cursor",
    "references",
    "language-server",
    "mcp-server",
    "semantic-code",
    "find-references",
    "csharp",
    "typescript",
    "python",
    "java",
    "go",
    "code-actions",
    "vscode"
  ],
  "engines": {
    "vscode": "^1.100.0"
  },
  "extensionKind": [
    "ui"
  ],
  "main": "./dist/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "mcpServerDefinitionProviders": [
      {
        "id": "vscode-lsp-mcp-bridge.provider",
        "label": "VS Code LSP MCP Bridge"
      }
    ],
    "commands": [
      {
        "command": "vscode-lsp-mcp-bridge.openQuickAccess",
        "title": "LSP MCP Bridge: Open Quick Access",
        "category": "LSP MCP Bridge"
      },
      {
        "command": "vscode-lsp-mcp-bridge.start",
        "title": "LSP MCP Bridge: Start Server",
        "category": "LSP MCP Bridge"
      },
      {
        "command": "vscode-lsp-mcp-bridge.stop",
        "title": "LSP MCP Bridge: Stop Server",
        "category": "LSP MCP Bridge"
      },
      {
        "command": "vscode-lsp-mcp-bridge.showStatus",
        "title": "LSP MCP Bridge: Show Status",
        "category": "LSP MCP Bridge"
      },
      {
        "command": "vscode-lsp-mcp-bridge.useWorkspace",
        "title": "LSP MCP Bridge: Route Gateway To This Workspace",
        "category": "LSP MCP Bridge"
      },
      {
        "command": "vscode-lsp-mcp-bridge.enableWriteTools",
        "title": "LSP MCP Bridge: Enable Write Tools",
        "category": "LSP MCP Bridge"
      },
      {
        "command": "vscode-lsp-mcp-bridge.disableWriteTools",
        "title": "LSP MCP Bridge: Disable Write Tools",
        "category": "LSP MCP Bridge"
      },
      {
        "command": "vscode-lsp-mcp-bridge.copyClientConfig",
        "title": "LSP MCP Bridge: Copy MCP Client Config",
        "category": "LSP MCP Bridge"
      },
      {
        "command": "vscode-lsp-mcp-bridge.openClientConfig",
        "title": "LSP MCP Bridge: Open MCP Client Config File",
        "category": "LSP MCP Bridge"
      }
    ],
    "walkthroughs": [
      {
        "id": "vscode-lsp-mcp-bridge.getStarted",
        "title": "Get Started with LSP MCP Bridge",
        "description": "Expose the current VS Code workspace language features to AI coding tools through MCP.",
        "steps": [
          {
            "id": "vscode-lsp-mcp-bridge.checkStatus",
            "title": "Check the local bridge",
            "description": "Open a workspace with the language extension you want to use, then run the status command to confirm the local MCP endpoint is available.\n\n[Show bridge status](command:vscode-lsp-mcp-bridge.showStatus)"
          },
          {
            "id": "vscode-lsp-mcp-bridge.copyConfig",
            "title": "Copy an MCP client config",
            "description": "Copy a ready-to-use MCP server configuration for Codex, VS Code/GitHub Copilot, Claude Code, or another HTTP MCP client.\n\n[Copy MCP client config](command:vscode-lsp-mcp-bridge.copyClientConfig)"
          },
          {
            "id": "vscode-lsp-mcp-bridge.useWorkspace",
            "title": "Route the gateway to this workspace",
            "description": "External MCP clients use the stable gateway endpoint. When several VS Code windows are open, route new external-client sessions to the current workspace.\n\n[Route gateway to this workspace](command:vscode-lsp-mcp-bridge.useWorkspace)"
          },
          {
            "id": "vscode-lsp-mcp-bridge.openConfigFile",
            "title": "Open an MCP client config file",
            "description": "Open a common MCP client configuration file and copy the matching bridge snippet to the clipboard. Missing files are created only after confirmation.\n\n[Open MCP client config file](command:vscode-lsp-mcp-bridge.openClientConfig)"
          },
          {
            "id": "vscode-lsp-mcp-bridge.enableWrites",
            "title": "Keep writes explicit",
            "description": "Read-only tools work by default. Write tools stay disabled until `vscodeLspMcpBridge.enableWriteTools` is enabled, and every write still asks for a VS Code approval before applying edits."
          }
        ]
      }
    ],
    "configuration": {
      "title": "VS Code LSP MCP Bridge",
      "properties": {
        "vscodeLspMcpBridge.autoStart": {
          "type": "boolean",
          "default": true,
          "description": "Start the local bridge server when VS Code finishes startup."
        },
        "vscodeLspMcpBridge.host": {
          "type": "string",
          "default": "127.0.0.1",
          "description": "Host for the local bridge server. Keep this on localhost."
        },
        "vscodeLspMcpBridge.port": {
          "type": "number",
          "default": 36521,
          "minimum": 1024,
          "maximum": 65535,
          "description": "Stable local gateway port for external MCP clients. VS Code/GitHub Copilot auto-registration uses the current window endpoint."
        },
        "vscodeLspMcpBridge.connectionFile": {
          "type": "string",
          "default": "",
          "description": "Optional path for the connection file. Empty uses ~/.vscode-lsp-mcp-bridge/connection.json."
        },
        "vscodeLspMcpBridge.enableWriteTools": {
          "type": "boolean",
          "default": false,
          "scope": "application",
          "description": "Enable MCP tools that can apply workspace edits, such as code actions, rename, and formatting. Each write still requires a VS Code approval prompt."
        },
        "vscodeLspMcpBridge.notificationDurationMs": {
          "type": "number",
          "default": 5000,
          "minimum": 0,
          "description": "How long extension status notifications stay visible in the status bar, in milliseconds."
        }
      }
    }
  },
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": "limited",
      "description": "The bridge refuses to start unless the workspace is trusted."
    }
  },
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\"",
    "typecheck": "tsc -p ./ --noEmit",
    "bundle": "esbuild src/extension.ts --bundle --platform=node --format=esm --target=node20 --external:vscode --outfile=dist/extension.js --sourcemap",
    "compile": "npm run clean && npm run typecheck && npm run bundle",
    "watch": "esbuild src/extension.ts --bundle --platform=node --format=esm --target=node20 --external:vscode --outfile=dist/extension.js --sourcemap --watch",
    "vscode:prepublish": "npm run compile",
    "package": "vsce package",
    "publish": "vsce publish"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@types/vscode": "^1.100.0",
    "@vscode/vsce": "^3.4.0",
    "esbuild": "^0.28.1",
    "typescript": "^5.8.0"
  }
}
